# Local dev config, committed on purpose — matches db/docker-compose.yml defaults.
# Not for secrets: staging/prod set DATABASE_URL as a real environment variable,
# which always takes precedence over this file.

DATABASE_URL=postgresql+psycopg://danslafouleapp:danslafoule@localhost:5432/danslafoule
SQL_ECHO=true
