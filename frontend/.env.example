# Absolute backend URL for the Capacitor native build (the WebView is not
# same-origin with any backend). Leave unset for the web build/dev server —
# it uses relative "/api/..." paths (Vite dev-server proxy or same-origin
# hosting by the FastAPI backend in prod).
# VITE_API_BASE_URL=https://your-backend-host
