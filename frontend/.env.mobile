# Loaded by `npm run build:mobile` (Vite `--mode mobile`). Replace with the
# real backend URL reachable from a phone/emulator before building — see
# frontend/.env.example.
VITE_API_BASE_URL=http://10.0.2.2:8000
